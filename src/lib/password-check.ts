/**
 * The live password check's address, shared by the Better Auth plugin that
 * answers it and the form that asks (ADR 0008). The verdicts it answers with
 * are named in `src/domain/passwords.ts`.
 */
export const PASSWORD_CHECK_PATH = "/password/check";
