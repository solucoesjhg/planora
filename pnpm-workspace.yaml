# Which dependencies may run install scripts. esbuild needs its own — drizzle-kit
# loads the schema through it — and nothing here uses sharp or unrs-resolver.
allowBuilds:
  esbuild: true
  sharp: false
  unrs-resolver: false
